<!DOCTYPE html>
<html lang="en">
  <head>
    <title><%= title %></title>
    <link rel="stylesheet" href="/stylesheets/style.css" />
  </head>
  <body>
    <header>
      <a href="<%= formLink %>">go to form</a>
    </header>

    <h1><%= title %></h1>
    <p>Welcome to <%= title %></p>

    <% for(let i = 0; i < messages.length; i++) { %>
    <p><%= messages[i].text %></p>
    <p>by : <%= messages[i].user %></p>
    <p>at : <%= messages[i].added %></p>
    <button id="<%= messages[i]._id %>" class="remove-msg" type="button">
      delete
    </button>
    <% } %>
  </body>
  <script async defer src="/javascripts/index.js"></script>
</html>
